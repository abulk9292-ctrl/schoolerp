from django.apps import AppConfig


class SchoolAssetsConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "school_assets"
    verbose_name = "School Assets / Room Monitor"
